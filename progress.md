## CS Fundamentals Log

| Date | Subjects Covered | Questions Asked |
|------|-------------------|------------------|
| 2026-08-27 | OOP, System Design, Computer Architecture | 1) What is polymorphism in object-oriented programming, and what is the difference between compile-time (static) and runtime (dynamic) polymorphism? 2) Compare horizontal scaling and vertical scaling — what are the tradeoffs of each? 3) A program iterates over a large 2D array with the inner loop varying the row index (column-major access) in a language that stores arrays in row-major order. Even though the whole array fits in L2 cache, cache misses are far higher than expected — why, and how would you fix it? |
